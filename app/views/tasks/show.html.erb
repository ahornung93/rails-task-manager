<h1><%= @task.title %></h1>

<div class="card">
  <p><%= @task.details %></p>
</div>

<p><%= @task.completed %></p>

<%= link_to 'Edit task', "#{params[:id]}/edit" %>

<%= link_to 'Back to list', tasks_path %>
